use imgal::simulation::blob::logistic_metaballs;

use cellcast::models::stardist_2d_versatile_fluo;

// blob simulation parameters
const SHAPE: (usize, usize) = (256, 256);

#[test]
fn stardist_2d_label_image() {
    todo!();
}
